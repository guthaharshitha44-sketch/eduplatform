# EduPath environment
# Optional: enables live AI generation (coach, practice, resume insights).
# Without a key, EduPath runs fully on its built-in deterministic engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
